{
  "mcpServers": {
    "loom": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/index.js"],
      "env": {
        "LOOM_WS_PORT": "${user_config.ws_port}"
      }
    }
  }
}
